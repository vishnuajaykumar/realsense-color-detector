version: "3.8"

services:
  detector:
    build:
      context: ..   # ~/ros2_ws/src/ — includes both packages
      dockerfile: realsense_color_detector/Dockerfile
    network_mode: host
    privileged: true
    pid: host
    devices:
      - /dev/bus/usb:/dev/bus/usb
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - /home/vish/.Xauthority:/root/.Xauthority
      - ./presentation/config:/ros2_ws/src/realsense_color_detector/presentation/config
      - /home/vish/.cache/ros_detector:/root/.cache
    environment:
      - DISPLAY=:0
      - XAUTHORITY=/root/.Xauthority
      - ROS_DOMAIN_ID=0
      - PYTHONUNBUFFERED=1

  mcp_server:
    image: python:3.11-slim
    working_dir: /app
    volumes:
      - ${HOME}/ros_mcp_server:/app
    command: >
      sh -c "pip install --no-cache-dir -e . opencv-python-headless && python server.py"
    network_mode: host
    depends_on:
      - detector
